import { QrScreen } from "@/components/display/QrScreen";

/**
 * Pagina da proiettare prima (e tra un set e l'altro) per invitare il
 * pubblico a scrivere. Quando e' il momento, si passa a `/display`.
 */
export default function QrPage() {
  return <QrScreen />;
}
